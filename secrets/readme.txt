One file per-tool. Use gitignore to make sure the unencrypted version isn't 
added to git. 

Decryption script goes in here; installation process runs the script, which 
auto-decrypts each file separately. Password prompt should happen early on.